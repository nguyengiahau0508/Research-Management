
export const environment = {
  production: false,  // Đánh dấu đây là môi trường development
  apiUrl: 'http://localhost:8080/api', // URL của Backend API (NodeJS, NestJS,...)
  authTokenKey: 'auth-token', // Key dùng để lưu JWT token trong localStorage
};
